<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Usuarios</h1>
    <table>
        <tr>
            <th>Nome</th>
            <th>Email</th>
            <th></th>
        </tr>
        <% for (let i=0; i < usuarios.length;i++ ) { %>
        <tr>
            <td>
                <%= usuarios[i].nome %>
            </td>
            <td>
                <%= usuarios[i].email %>
            </td>
            <td> 
                <a href="/usuarios/alterar_dados/<%= usuarios[i].id %>">Alterar dados</a>
                <a href="/usuarios/alterar_senha/<%= usuarios[i].id %>">Alterar senha</a>
            </td>
        </tr>
        <% } %>
                
    </table>
</body>
</html>